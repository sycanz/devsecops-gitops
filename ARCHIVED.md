# Archived

This repo has been superseded by [`homelab-gitops`](../homelab-gitops).
The 7 Kyverno ClusterPolicies, the ArgoCD App-of-Apps + sync-wave pattern,
and the monitoring Helm values were carried forward unchanged (module
`kyverno.io`, sync waves -1/5, ServerSideApply). The stg/prd environment
split was dropped — the homelab is a single k3s cluster/environment.

See `ECOSYSTEM-PLAN.md` in the projects root for the full plan. Kept for
history, not deleted.
